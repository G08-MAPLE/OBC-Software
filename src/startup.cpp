#include "startup.hpp"
#include "main.hpp"
// #include "state_machine.hpp"

void startup(void * param){
    // StateMachine dartStateMachine;

    for(;;){
        if (state == State::BOOT) {
            
        }
    }
}